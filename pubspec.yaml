name: internal_inventory_tracker
description: Flutter frontend for IT Management System

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.1.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0
  intl: ^0.18.1
  pull_to_refresh: ^2.0.0
  csv: ^5.0.3
  #file_saver: 0.1.1
  path_provider: ^2.1.1  
  permission_handler: ^11.0.1
  pdf: ^3.10.4
  printing: ^5.9.2
  fl_chart: ^0.66.0 
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true